from typing import Dict, List, Tuple

Command = List[str]
DataType = str
ExePath = str
FilePath = str
FileFormat = str
Model = str
Newick = str
TreeIndex = int
TreeTreeIndexed = Dict[Tuple[TreeIndex, TreeIndex], float]
